# Steps to setup a new repository

1. Make sure you are in proper folder in VS code , File > Open Folder
2. Open command prompt with View > Terminal > cmd
3. uv init
4. uv venv
5. .venv\Scripts\activate
6. uv add -r requirements.txt
7. git branch -M main
